//Operador ternário
//abreviação de if else

//(condição) ? true : false

//sempre retorna valores booleanos

var idade = 19;
var naoEstaDirigindo = false;

var podeBeber = (idade >= 18 && naoEstaDirigindo) ? 'Pode beber' : 'Não pode beber';
console.log(podeBeber);